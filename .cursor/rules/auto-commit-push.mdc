---
description: 代码更新完成后自动 git commit 并 push 到 game2 远程仓库
alwaysApply: true
---

# 自动提交并推送

每次完成代码修改后，**必须**直接执行 git 提交并推送到远程，**无需用户提醒，也无需向用户确认**。

## 执行原则

- git 相关操作（`git add` / `git commit` / `git push`）**直接执行**，不要询问「是否提交/推送」
- 不要为 git 操作请求额外审批；遇到 push 失败自行按失败处理流程重试
- push 前如存在无效 `GH_TOKEN` 环境变量，先清除，优先使用 `gh auth login` 的 keyring 凭证

## 触发时机

- 完成一组有意义的代码改动后（功能、修复、配置、前端页面等）
- 不要在仅阅读/回答问题、未改文件时提交
- 不要创建空提交

## 提交流程

1. 并行执行：`git status`、`git diff`、`git log -3 --oneline`
2. 只 `git add` 相关源码文件，**禁止**提交：
   - `BOOT-INF/`、`target/`、`.env`、密钥/凭证文件
3. 写 1～2 句 commit message，说明 why
4. `git commit`
5. `git push -u origin HEAD`（或 `git push origin main`）

## 远程仓库

- 默认 remote：`origin` → `https://github.com/eternaltime66-del/game2.git`
- 默认分支：`main`

## 失败处理

- 远程仓库不存在：提示用户在 GitHub 创建 `game2` 空仓库后重试 push
- push 被拒绝：先 `git pull --rebase` 再 push，不要 force push 除非用户明确要求
- 无改动：跳过 commit/push
