---
description: 每完成 5 条用户指令后 batch git commit 并 push 到 game2
alwaysApply: true
---

# 批量提交并推送（每 5 次）

**不要**在每次改完代码后立即 commit/push。改为：**每完全处理结束 5 条用户指令**，再统一提交并推送。

## 什么算「处理完毕」

- 用户发来一条指令（一个独立需求/任务）
- 你已将该指令**完整处理结束**（实现、修复、配置等做完，并给出结果）
- 仅回答问题、未改代码也算 1 次（若该轮对话无代码变更则只计数，不 commit）

## 计数规则

状态文件：`.cursor/git-batch-state.json`

```json
{ "batchSize": 5, "completedCount": 0 }
```

每次**完全处理结束**一条用户指令后：

1. 读取 `completedCount`，加 1 写回
2. 若 `completedCount < batchSize`：**不 commit/push**，仅在回复中可简短说明进度（如 `git 计数 2/5`）
3. 若 `completedCount >= batchSize`：执行下方提交流程，然后将 `completedCount` 重置为 `0`

## 执行原则

- 达到 5 次时 git 操作**直接执行**，无需用户确认
- 不要为 git 操作请求额外审批；push 失败按失败处理流程重试
- push 前如存在无效 `GH_TOKEN`，先清除，优先使用 `gh auth login` 的 keyring

## 提交流程（仅计数满 5 时）

1. 并行执行：`git status`、`git diff`、`git log -3 --oneline`
2. 只 `git add` 相关源码文件，**禁止**提交：
   - `BOOT-INF/`、`target/`、`.env`、密钥/凭证文件
3. 写 1～2 句 commit message，概括这 5 轮内的主要改动
4. `git commit`（含 `.cursor/git-batch-state.json` 的重置状态）
5. `git push origin main`

## 远程仓库

- 默认 remote：`origin` → `https://github.com/eternaltime66-del/game2.git`
- 默认分支：`main`

## 失败处理

- 无改动：重置计数但不创建空提交
- push 被拒绝：先 `git pull --rebase` 再 push，不要 force push 除非用户明确要求
